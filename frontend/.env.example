# Base URL of the backend API.
# Local dev (default if unset):
VITE_API_BASE=http://localhost:8000/api

# When the backend runs on Google Colab behind an ngrok tunnel, use the
# public URL that colab_server.py prints, e.g.:
# VITE_API_BASE=https://xxxx-xx-xxx-xx-xx.ngrok-free.app/api
